{
  "name": "lcdd-monorepo",
  "private": true,
  "version": "0.5.0",
  "description": "Living Context Driven Development — Reference Implementation",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@lcdd/core && npm run build --workspace=@lcdd/cli && npm run build --workspace=@lcdd/mcp",
    "test": "npm run test --workspaces",
    "docs:check": "node scripts/check-doc-consistency.mjs",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "openai": "^4.104.0"
  }
}
