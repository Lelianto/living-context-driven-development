{
  "name": "lcdd-monorepo",
  "private": true,
  "version": "0.6.0",
  "description": "Living Context Driven Development — Reference Implementation",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@lcdd/core && npm run build --workspace=@lcdd/cli && npm run build --workspace=@lcdd/mcp",
    "test": "npm run test --workspaces",
    "docs:check": "node scripts/check-doc-consistency.mjs",
    "readmes:check": "node scripts/check-package-readmes.mjs",
    "governance:check": "node scripts/check-self-governance.mjs",
    "release:local": "node scripts/prepare-local-release.mjs",
    "verify:release": "npm run docs:check && npm run readmes:check && npm run build && npm run governance:check && npm test && npm audit --omit=dev",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "openai": "^4.104.0"
  }
}
